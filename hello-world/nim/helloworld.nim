# Requires nim: http://nim-lang.org/
# Run in terminal: nim c -r --verbosity:0 helloworld
#
# Note, the name of the file matters (dashes aren't allowed in nim file names because the file name
# becomes a nim module, and dashes aren't allowed in nim module names.

echo "hello world"